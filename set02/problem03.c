#include<stdio.h>
int input_number();
int is_composite(int n);
void output(int n, int result);

int input_number() {
    int num;
    printf("Enter the number:");
    scanf("%d",&num);
}

int is_composite(int n) {
    for(int i = 2) {

    }
}